# models/product_similarity.pkl is now tracked (compressed to 35.6MB)
# models/product_similarity.pkl

# ── Secrets & credentials (NEVER commit these) ─────────────────────────────
.streamlit/secrets.toml
.env
.env.*
*.pem
*.key